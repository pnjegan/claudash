# Screenshot Instructions

1. Run: `python3 cli.py dashboard`
2. Open http://localhost:8080
3. Take screenshot of Personal (Max) tab showing:
   - API equiv, ROI, cache hit rate
   - Project table
   - Insight cards
4. Save as `docs/screenshot.png`
